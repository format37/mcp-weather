FROM python:3.11

WORKDIR /app
COPY mcp_simple_auth/ /app/mcp_simple_auth/
COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

CMD ["python", "-m", "mcp_simple_auth.auth_server", "--port=9001"]